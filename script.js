let elem = document.querySelectorAll('.piano-key');
elem.forEach(elem => {
    elem.style.backgroundColor = 'yellow';
});
let elem_sharp = document.querySelectorAll('.sharp');
elem_sharp.forEach(query => {
    query.style.backgroundColor = 'red';
});